#pragma once

// En gros si je mets ça c'est pour éviter qu'il devienne trop gros comme Alexandre :D, il est bloqué à 100
int snakeX[100], snakeY[100];
// Snake taille
int snakeLength;
int fruitX, fruitY;
int score;
int direction;
int gameOver;
